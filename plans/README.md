# Implementation Plans

Generated by the improve skill on 2026-09-18 against commit `efd5c70`, from a portfolio-wide SEO/AEO review that used Search Console, Bing Webmaster and GA4 data from the analytics hub. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

This repo has no test, lint or typecheck scripts. `npm run build` plus greps over `dist/` are the verification gates in every plan.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Every page has one URL, the one with the trailing slash | P1 | S | — | TODO |
| 002  | llms.txt lists the guides, and every production deploy pings IndexNow | P2 | S | 001 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 002 requires 001 because the llms.txt links it adds use the slash URLs that 001 makes canonical everywhere.

## Direction (not planned)

- **More task pages for "basketball video analysis software".** The homepage holds that query at position 22 with 120 impressions in 28 days, and the site has only seven indexable pages. More pages on specific coaching tasks (a scouting report template, shot charts from film, breaking down a practice) are the likely way up, the same pattern that works for the portfolio's therapy-resources site. The topics are the owner's call, so there's no plan until they pick them.

## Findings considered and rejected

- **Retitle for "basketball video breakdown software" (#3, 134 impressions, 0 clicks).** Rejected: seven different pages all sit at position 3.3 to 3.7 for that query, which is the pattern of an AI Overview citation block, not a snippet people skip. A title change wouldn't move clicks.
- **The site "has duplicate content across www and apex".** Not a problem: `https://www.basketballvideoanalyzer.com/` already 308-redirects to the apex.
